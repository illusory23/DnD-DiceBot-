2026-08-27_25_地图角色信息面板负重速度同步

问题：
地图（/map）左上角 token 角色信息面板显示的速度为基础速度，
未应用负重规则更新后的减速效果（角色卡左栏已实现）

修复（web/static/map.js）：
- 新增顶层函数 tokenCarrySpeed(charData)：与角色卡一致的 5e 变体负重规则
  （总负重 = 物品×数量 + 武器 + 护甲 + 盾；上限 = 力量×15；
  >力量×5 → 🟡负重(-10尺)；>力量×10 → 🔴严重负重(-20尺)；超重 → 拖拽 5 尺）
- token 信息面板速度行应用减速：显示实际速度 + 负重状态标记
  （如「🏃 20尺 🟡负重」，红色标注；正常无标记）
- map.js v83 → v84（缓存版本号同步）

验证：
- JS node --check 通过
- 计算逻辑与角色卡 calcCarry 一致（力量14：阈值70/140/210 磅）

涉及文件：
- web/static/map.js（v84）、web/templates/map.html（v=84）

需要重启服务器生效（静态资源靠版本号刷新）。
