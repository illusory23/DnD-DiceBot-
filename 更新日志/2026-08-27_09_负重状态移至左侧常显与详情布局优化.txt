2026-08-27_09_负重状态移至左侧常显与详情布局优化

功能：
1. 角色详情「总负重 / 负重状态 / 负重效果」移至左侧基础信息区常显
   （原在「🎒 背包物品 & 钱币」折叠分区内，不展开看不到）
   - 左栏「抗性」下方新增「🎒 负重: X / Y 磅」行：
     正常显示绿色「正常」；超重（>力量×15 磅）显示红色数值 + 「⚠️ 超重！无法携带更多」
   - 背包物品区原有负重条保留（同一变量）
2. 角色详情双栏布局优化：左侧加宽、右侧词条区压缩
   - .char-detail-grid 列宽 230px 1fr → minmax(300px, 38%) 1fr
   - 左侧（头像/HP/属性/抗性/负重/快捷数值）不再拥挤

实现：
- web/static/character.js：负重计算（力量×15 上限、总负重=物品×数量+武器+护甲+盾）
  提前到渲染前，左栏与物品区共用；左栏新增负重状态行
- web/static/style.css：char-detail-grid 列宽调整
- character.js v22 → v23

验证：
- JS node --check 通过
- 负重计算逻辑此前已测试（力量14 → 上限210磅）

涉及文件：
- web/static/character.js（v23）、web/static/style.css、web/templates/character.html（v=23）

需要重启服务器生效；style.css 无版本号，浏览器需强刷（Ctrl+F5）刷新样式缓存。
