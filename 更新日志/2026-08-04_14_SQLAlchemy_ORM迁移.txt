✅ 完成 SQLAlchemy ORM 迁移（PostgreSQL 路线第一步）

📁 新增:
  • config.py — 应用配置（DATABASE_URL 环境变量切换数据库）
  • core/database.py — SQLAlchemy 引擎/会话统一管理
  • core/models.py — 15张表完整 ORM 模型 + 级联关系

📝 重写:
  • core/character.py (1864行 → 600行) — 52个函数全部从 raw sqlite3 改为 ORM

📝 修改:
  • web/app.py — 集成 ORM 初始化
  • requirements.txt — flask-sock 已在上次加入

📊 验证:
  • 15张表完整创建
  • 22个角色 / 37把武器 / 100件物品数据完好
  • CRUD 全部测试通过

🔀 切换到 PostgreSQL:
  export DATABASE_URL="postgresql://user:pass@localhost:5432/dicebot"
  → 只需一行环境变量，代码零改动

🗑 消除的技术债:
  • PRAGMA table_info + ALTER TABLE 手写迁移 → SQLAlchemy create_all
  • get_db() 每次新建连接 → 连接池复用
  • 手动逐表级联删除 → ORM cascade='all, delete-orphan'
  • 手写 SQL 字符串 → ORM 类型安全查询
  • sqlite3.Row dict 转换 → _char_to_dict() 类型明确
