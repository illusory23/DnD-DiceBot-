2026-08-09 更新 · 管理员后台搭建与增强
═══════════════════════════════════════════

一、管理员后台整体框架（web/admin.py + templates/admin/ + static/admin/）
───────────────────────────────────────────
• 独立 Blueprint 挂载 /admin/*，页面路由与 API 统一 admin_required 鉴权
• 登录/登出（session 鉴权，审计日志记录管理员登录）
• 9 大模块：系统总览仪表盘 / 用户管理 / 角色管理 / 聊天管理 /
  统计中心 / 错误监控 / 系统维护 / 内容管理 / 角色与用户详情页

二、本轮修复的问题
───────────────────────────────────────────
• 补齐 6 个缺失/不匹配 API（原模板调用路径在后端不存在，全部 404）：
  - GET/DELETE /admin/api/chat-archives  聊天归档查询与清理
  - DELETE /admin/api/chat/messages      清空聊天消息
  - POST   /admin/api/archive-stats      统计快照归档
  - GET/DELETE /admin/api/frontend-errors 前端错误日志查询/清空
• 角色详情页空数据：Character 模型无 to_dict()，改为手动构造 32 字段
• 用户"最后登录"不显示：User.to_dict() 补充 last_login 字段

三、新增 · 用户管理增强
───────────────────────────────────────────
• users 表新增列（启动时自动补列，旧库兼容）：
  - is_active        账号启用/禁用状态
  - last_login_ip    最后登录 IP
• 用户列表/详情页新增：禁用/启用账号、重置密码、删除账号（角色卡保留）
• 前台 /api/auth/login 校验 is_active，禁用账号无法登录
• 角色详情页新增管理员编辑功能：名称/等级/HP/AC/XP/属性等 22 个白名单字段
  （服务端类型与范围校验，所有操作写入审计日志）

四、新增 · 审计日志页（/admin/audit）
───────────────────────────────────────────
• 查看 logs/audit.log 关键操作记录（管理员登录、用户/角色变更、备份等）
• 支持按操作类型、用户名、关键词筛选，100~1000 条可选

五、新增 · 数据备份中心（/admin/backups）
───────────────────────────────────────────
• 一键备份：数据库 + 聊天记录/归档 + 统计 + 画布 + 日志 → backups/
• 数据库使用 sqlite3 在线备份 API（服务器运行中也能安全备份，Windows 无文件占用问题）
• 备份列表（大小/时间/文件数）、下载（zip 打包）、删除
• 恢复备份：恢复前自动备份当前状态到 backups/pre_restore_*（安全网）
• 日志清理：删除 logs/ 下超过 N 天的轮转日志文件

六、正反代码检查与修复（2026-08-09 补充）
───────────────────────────────────────────
重大数据修复：
• users 表长期缺失 is_admin 列（models 加了字段但库从未补列，任何用户查询
  都会报错）——启动补列逻辑已加入 is_admin，实测修复
• SECRET_KEY 从公开硬编码改为 data/secret_key 随机持久化密钥（防会话伪造）

安全修复（独立反面审查 + 26 项边界测试验证）：
• 用户名注入 XSS：注册/改用户名接口加白名单（中文/字母/数字/下划线/连字符），
  存量 10 个用户名已验证合规；后台用户详情页 phone/bio 补转义
• 禁用语义统一：后台登录与鉴权同样校验 is_active（禁用=前台后台都不能登录）
• 布尔解析：is_admin/is_active 接受字符串"true/false"，杜绝 "false"→True 反向提权
• 分页边界：page/per_page 越界（0、负数、超大）clamp，不再 500
• 邮箱校验：拒绝空邮箱（避免唯一约束 500）
• 删除用户时显式清理北境存档（SQLite 外键未启用，防孤儿数据）
• 备份恢复后清空 SQLAlchemy 会话缓存 + 前端提示重启服务器
• 事件统计归档加锁（防并发竞态）
• 审计日志解析正则收紧（ip 不含空格），密码重置不再明文记录

七、初始管理员账号
───────────────────────────────────────────
用户名: IllusoryActer    密码: illusorior
（已设置 is_admin + is_active，后台登录已验证通过）

八、改动文件
───────────────────────────────────────────
• web/admin.py        （+约 500 行：新 API + 页面路由 + 安全修复）
• web/app.py          （补列逻辑、SECRET_KEY、用户名校验、登录校验、登录 IP、事件锁）
• core/models.py      （User 新列 + to_dict 扩展）
• templates/admin/    （base 导航、audit/backups 新页面、users/user_detail/character_detail 增强、errors 路径修正）
• static/admin/admin.css（select 样式补充）
• data/secret_key     （首次启动自动生成）
