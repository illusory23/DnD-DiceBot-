2026-08-14 第22次更新：背包物品读取 + 法术位数量语义修改

【1. 背包物品读取】
问题：导入只读取主要表装备区的奇物，没有读取「背包」sheet 中的物品。
修改：
- _import_inventory_sheet 描述列兼容 F 列（DND5E24 模板）与 I 列（NekoWorks）
- 主函数物品合并改为"背包sheet物品 + 主要表装备区物品"去重合并
  （之前是背包物品覆盖主要表物品）
验证：麻绳/火把/铁匠工具（背包sheet）+ 褪色丝带/饱经磨损的长袍（主要表）
      共5件全部导入 ✅

【2. 法术位数量语义修改（用户需求）】
原逻辑：used=已消耗数量（初始0），显示"已用0/最大2"
新逻辑：used=当前拥有的数量（初始=最大，全满），施法后减少
修改（core/character.py）：
- init_spell_slots：初始 used=count（全满）
- import_from_excel_data：导入法术位 used=max（全满）
- use_spell_slot：消耗改为 used-1（当前减少），剩余=used
- long_rest：长休恢复 used=max（全满）
- web/app.py 保存法术位 API：限制 0-max 不变（手动输入当前数量）

验证（端到端实测）：
- 导入后 1环法术位显示 2/2（当前2/最大2）✅
- 施法消耗后 remaining=1（当前1）✅
- 长休恢复全满 ✅

【说明】
- 旧角色（此前导入/创建的）法术位数据是旧语义（已消耗），
  可在角色卡法术位输入框手动调整为当前数量后保存
