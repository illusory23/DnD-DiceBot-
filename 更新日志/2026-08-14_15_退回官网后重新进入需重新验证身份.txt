2026-08-14 第15次更新：退回官网后重新进入跑团平台需重新验证身份

【需求】
- 从外部（官网）进入跑团平台：显示身份选择界面，重新验证
- 平台内切换功能页面：不验证、不弹窗、身份保持

【问题】
同标签页导航时 sessionStorage 保留，退回官网（/ 根路径）再进入
平台页面时仍自动恢复会话，不弹出身份选择界面。

【修改】
web/templates/portal/index.html（官网首页）和 portal/user.html（用户中心）：
- 页面加载时清除跑团会话 sessionStorage.removeItem('dnd_joined_room')
- 从官网再次进入跑团平台时无会话 → 自动显示身份选择界面

【验证】（Playwright 无头浏览器实测）
- 加入后会话存在 ✅
- 退回官网（/）→ 会话被清除 ✅
- 重新进入聊天室 → 覆盖层显示，重新验证 ✅
- 平台内切换（聊天室→地图→聊天室）→ 不弹窗、DM按钮保持 ✅
