2026-08-27_08_护甲类型下拉去除盾

功能：
角色卡护甲类型下拉去掉"盾"选项（下方已有独立盾填写区，避免重复）

实现：
- web/static/character.js：ARMOR_TYPES 去掉"盾"
- 兼容：旧数据 armor_type='盾' 不在选项中时自动追加为选中项（原有逻辑），不丢值
- character.js v21 → v22（缓存版本号同步）

验证：
- JS node --check 通过

需要重启服务器生效（静态资源靠版本号刷新，模板无改动——重启后刷新页面即可）。
