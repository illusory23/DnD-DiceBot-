2026-08-25_5_死亡豁免播报与信息面板增强
═══════════════════════════════════════════

【修复】死亡豁免判定后成功/失败次数不增加
  根因排查：后端 death-save API 实测正常（次数正确增加并持久化）；
  前端流程静态分析无断点 → 最可能为服务器未重启导致 API 404
  （Flask 返回 HTML 错误页），原代码 `resp.json()` 解析 HTML 抛
  异常后被 catch 静默吞掉——用户无任何提示、次数不增加。
  修复（web/static/map.js maybeTriggerDeathSave）：
    · `resp.ok` 检查，非 200 时 alert 明确提示（HTTP 状态 + 响应
      片段），不再静默失败
    · `resp.json()` 包裹 try-catch，失败 alert"服务器需重启以启用
      该功能"
    · `total` 校验（1-20），异常值 alert 提示
  顺带修复：roll3dStage 内部按钮文案重置会覆盖死亡豁免专用按钮
  （"🎲 掷死亡豁免"被改回"🎲 掷骰"）——roll3dStage 增加 btnLabel
  参数，死亡豁免调用传 '🎲 掷死亡豁免'

【优化 1】死亡豁免结果聊天室播报全覆盖（map.js）
    · 成功：💀 xxx 死亡豁免成功（15），成功 1/3
    · 失败：💀 xxx 死亡豁免失败（8），失败 1/3
    · 大失败：💀 xxx 死亡豁免掷出 1（大失败）！失败 ×2（2/3）
    · 稳定：💀 xxx 死亡豁免成功 3 次，稳定下来！（20 特判文案）
    · 死亡：💀 xxx 失血过多，无力支撑，宣告死亡...（原有）

【优化 2】地图 token 信息面板增强（showTokenInfoPanel）
    · 新增死亡豁免行：💀 死亡豁免 成功 x/3 | 失败 x/3
      + [稳定]/[死亡] 标记（角色无死亡豁免记录时不显示）
    · AC 行追加：✨ DC（法术DC spell_save_dc）| 👁️ 被动察觉
      （passive_perception）
    · 死亡豁免掷完后 fetchCharData 刷新角色缓存并重渲染选中的
      token 信息面板（次数实时更新）

【验证】
  · node --check map.js 通过
  · 后端 death-save API 复测正常（roll=15 → 成功 1、roll=8 →
    失败 1，GET 回读一致，测试角色已清理）
  · 播报/面板逻辑走查：所有分支文案完整、fetchCharData 后
    重渲染路径确认

【生效】仅前端（map.js + map.html，v80）→ 无需重启服务器，
      刷新页面即可；若此前死亡豁免上报不增加次数，请确认
      服务器已重启（需包含上轮 death-save API）
